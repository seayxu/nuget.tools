# ------------------------------------------------------------------------------
# <auto-generated>
#
#     This code was generated.
#
#     - To turn off auto-generation set:
#
#         [GitHubActions (AutoGenerate = false)]
#
#     - To trigger manual generation invoke:
#
#         nuke --generate-configuration GitHubActions_continuous.build --host GitHubActions
#
# </auto-generated>
# ------------------------------------------------------------------------------

name: continuous.build

on:
  push:
    paths:
      - '**'
      - '!.release'

jobs:
  windows-latest:
    name: windows-latest
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v2
      - name: Run './build.cmd Artifacts'
        run: ./build.cmd Artifacts
      - uses: actions/upload-artifact@v1
        with:
          name: artifacts
          path: artifacts
  ubuntu-latest:
    name: ubuntu-latest
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Run './build.cmd Artifacts'
        run: ./build.cmd Artifacts
      - uses: actions/upload-artifact@v1
        with:
          name: artifacts
          path: artifacts
  macOS-latest:
    name: macOS-latest
    runs-on: macOS-latest
    steps:
      - uses: actions/checkout@v2
      - name: Run './build.cmd Artifacts'
        run: ./build.cmd Artifacts
      - uses: actions/upload-artifact@v1
        with:
          name: artifacts
          path: artifacts
